---
// src/pages/about.astro
import Layout from "../layouts/BaseLayout.astro";
import { SITE, SOCIAL_LINKS } from "@config/site";
import TechLogoGrid from "@components/tech/TechLogoGrid.astro";
import { techLogos } from "@config/tech-logos";

const links = [
  { name: "ブログ", href: "/posts" },
  { name: "ポートフォリオ", href: "/use-cases" },
  { name: "GitHub", href: SOCIAL_LINKS.github },
].filter((l) => typeof l.href === "string" && l.href.length > 0);
---

<Layout title={`About - ${SITE.title}`} description={SITE.description}>
  <main class="container mx-auto px-4 py-12 sm:px-6 lg:px-8">
    <header class="max-w-2xl">
      <h1 class="text-3xl sm:text-4xl font-bold text-slate-100">自己紹介</h1>
      <p class="mt-4 text-base leading-relaxed text-slate-200/80">
        このサイトは、ブログ（学習ログ・制作メモ）とポートフォリオをまとめた個人サイト。
        小さく作って、少しずつ良くしていくための記録を置いている。
      </p>
    </header>

    <section class="mt-10 max-w-2xl">
      <h2 class="text-lg font-semibold text-slate-100">リンク一覧</h2>
      <ul class="mt-4 flex flex-wrap gap-3">
        {links.map((l) => (
          <li>
            <a
              href={l.href}
              class="rounded-lg border border-white/10 px-4 py-2 text-sm text-slate-200/80 hover:bg-white/5 hover:text-slate-100 transition-colors"
            >
              {l.name}
            </a>
          </li>
        ))}
      </ul>
    </section>

    <section class="mt-10 max-w-2xl">
      <h2 class="text-lg font-semibold text-slate-100">技術スタック</h2>
      <p class="mt-4 text-base leading-relaxed text-slate-200/80">
        Next.js / React / TypeScript / Tailwind CSS / GitHub / etc.
      </p>
      <TechLogoGrid logos={techLogos} />
      <p class="mt-3 text-xs text-slate-400/80">
        Tech logos inspired by <a href="https://github.com/SAWARATSUKI/KawaiiLogos" target="_blank" rel="noreferrer">KawaiiLogos by SAWARATSUKI</a>
      </p>
    </section>

    <section class="mt-10 max-w-2xl">
      <h2 class="text-lg font-semibold text-slate-100">特技</h2>
      <p class="mt-4 text-base leading-relaxed text-slate-200/80">
        タロットカード占い
      </p>
      <a href="https://www.uranai.org/tarot/" target="_parent"><img src="https://www.uranai.org/proof/tarot.png" width="40%" alt="タロットカード士®資格資格証明"></a>
    </section>
    <p class="text-xs text-slate-400/80">
        ※タロットカード士®は、一般社団法人日本占い師協会の登録商標です。<br>  
        ※このロゴは、一般社団法人日本占い師協会の許可を得て使用しています。
      </p>
  </main>
</Layout>
